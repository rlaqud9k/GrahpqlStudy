const byeong = (name, age, gender) => {
    name: name,
    age: age,
    gender: "gender"
};

const resolvers = {
    Query : {
        Person : (name, age, gender) =>
    }

}

export default resolvers;